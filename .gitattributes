# Default: auto line endings
* text=auto

# Force LF for shell scripts and Docker files (must be LF in Linux containers)
*.sh text eol=lf
docker-entrypoint.sh text eol=lf
Dockerfile text eol=lf
docker-compose*.yml text eol=lf
